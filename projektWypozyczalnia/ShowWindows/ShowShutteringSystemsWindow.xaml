<Window x:Class="projektWypozyczalnia.ShowWindows.ShowShutteringSystemsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dostępne systemy i rozmiary" Height="400" Width="500"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" 
                   Text="Wprowadzone systemy szalunkowe" FontSize="14" Margin="0,0,0,10"/>
        <DataGrid x:Name="ActiveShutteringSystemsDg"
                  AutoGenerateColumns="False"
                  CanUserResizeColumns="False"
                  CanUserResizeRows="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  GridLinesVisibility="All"
                  IsReadOnly="True"
                  VerticalScrollBarVisibility="Auto" 
                  HorizontalScrollBarVisibility="Auto"
                  Grid.Row="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="System" Binding="{Binding NameOfShutteringSystem}" Width="*"/>
                <DataGridTextColumn Header="Producent" Binding="{Binding Manufacturer}" Width="*"/>
                <DataGridTextColumn Header="Długość" Binding="{Binding Length}" Width="*"/>
                <DataGridTextColumn Header="Szerokość" Binding="{Binding Width}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>