<Window x:Class="projektWypozyczalnia.RentWindows.RentingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Wypożyczenia" Height="600" Width="1200"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Loaded="RentingsWindow_OnLoaded">
    <Grid>
                    <StackPanel Orientation="Horizontal" Margin="10,0,0,0" VerticalAlignment="Top">
                <Button x:Name="ExitFromRentalWindow"
                        Content="Wyjdź" 
                        Margin="10,15,0,0"
                        Click="Close_OnClick"/>
                <Button x:Name="RentShutterings" 
                        Content="Wypożycz szalunki" 
                        Margin="10,15,0,0"
                        Click="RentShutterings_OnClick"/>
                <Button x:Name="RentEquipment" 
                        Content="Wypożycz osprzęt" 
                        Margin="10,15,0,0"
                        Click="RentEquipment_OnClick"/>
                <Button x:Name="Refresh" 
                        Content="Odśwież" 
                        Margin="10,15,0,0"
                        Click="RefreshWindow_OnClick"/>
            </StackPanel>
            
            <DataGrid x:Name="RentsDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" Margin="10,50,10,10" IsReadOnly="True" CanUserResizeColumns="False" CanUserResizeRows="False" CanUserReorderColumns="False" >
                    <DataGrid.Columns>
                            <DataGridTextColumn Header="Numer umowy" Binding="{Binding NumberOfLend}" Width="*"/>
                            <DataGridTextColumn Header="Kontrahent" Binding="{Binding NameOfCompany}" Width="*"/>
                            <DataGridTextColumn Header="Adres budowy" Binding="{Binding AddressOfBuilding}" Width="*"/>
                            <DataGridTextColumn Header="Początek najmu" Binding="{Binding StartLendDate}" Width="*"/>
                            <DataGridTextColumn Header="Koniec najmu" Binding="{Binding EndLendDate}" Width="*"/>
                    </DataGrid.Columns>
                    <DataGrid.ContextMenu>
                            <ContextMenu>
                                    <MenuItem Header="Szczegóły" Click="DetailsMenuItem_Click"/>
                            </ContextMenu>
                    </DataGrid.ContextMenu>
            </DataGrid>
    </Grid>
</Window>
